#include <stdio.h>

//  As per C-standard function cannot be defined inside structure

struct Date
{
    int day_tms;
    int month_tms;
    int year_tms;

     // Function definition
     // struct (explained in session 09)
     // CTE

     /*
     int get_day()
     {
        return (day);
     }*/
};